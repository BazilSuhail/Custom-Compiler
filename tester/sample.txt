include<main>

// Function with type mismatches
int calculate(int a, int b) {
    string result = 100;                    // Error: int to string
    bool flag = 42;                         // Error: int to bool
    //return "text";                          // Error: string return
}

void voidFunc() {
    return 42;                              // Error: int return in void function
}

// Function call errors
float process(float x, bool y) {
    return x;
}

main {
    // Assignment type mismatches
    int num = "hello";                      // Error: string to int
    bool status = 3.14;                     // Error: float to bool
    
    // Function call errors
    int val1 = calculate(5);                // Error: missing parameter
    float val2 = process(10, true, "extra"); // Error: extra parameter
    int val3 = calculate(5, "text");        // Error: wrong parameter type
    
    // Boolean operation errors
    int x = 10;
    float y = 5.5;
    bool result1 = x && y;                  // Error: non-boolean operands
    bool result2 = x || true;               // Error: int in boolean op
    
    // Bitwise operation errors
    char ch = 'A';
    float f = 2.0;
    int bitwise = ch & f;                   // Error: float in bitwise
    int bitwise2 = ch << f;                   // Error: float in shift
    
    // Condition errors
    if (x) {                                // Error: int condition
        print("Wrong condition");
    }
    
    while (y) {                             // Error: float condition
        y = y - 1.0;
        break;                                  // Error: break outside loop
    }
    
    // Control flow errors
    break;                                  // Error: break outside loop
    
    // Arithmetic errors
    string text = "error";
    int calculation = text + 5;             // Error: string arithmetic


    float f_switch = 5.2;
    switch (f_switch) {               // switch on int
        case 5.2 {
            break;
        }            // ❌ Error: char doesn't match int
    }

    int case_switch = 5;
    switch (case_switch) {               // switch on int
        case 'A'{
            break;
        }            // ❌ Error: char doesn't match int
    }

    float m=5.30;
    m = m++;
    int a = ++a - m++;
    int b = !a;
    bool flag = true;
    bool result = !flag; // ✅ OK: NOT on boolean
}